{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window (Option B): UI is bundled as Tauri assets and reaches the kernel only through the app's own kernel_request command over loopback. Core default plus drag-region + toggle-maximize for the custom frameless chrome (window controls are the app's own win_* commands). No fs/shell grants — 'full system resources' here means compute (CPU/GPU/RAM via Ollama), not filesystem access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize",
    "notification:default"
  ]
}
